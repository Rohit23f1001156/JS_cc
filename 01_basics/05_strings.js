/*
rather than using + for concatenation we will be using string interpolation:
we use "  ` " to do it, (the symbol is to the left of 1)
*/
const name = "Rohit"
console.log(`Hello my name is ${name}`)
console.log(name.toUpperCase())