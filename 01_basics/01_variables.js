const accountId = 144553
let accountEmail = "rohit@g.com"
var accountPassword = "1234"

accountCity = "Jaipur"
 accountState;

console.table([accountId, accountEmail, accountPassword, accountCity, accountState])

/*
Now, only let and const are used, var is not used -> as scope was not defined for var, so any changes made to the var name meant change
to all the places that var occured!
const -> once set, cannot be changes later on this code!
if no value give to a variable (use let, for const value should be initiated!), then it is given the value "UNDEFINED"
*/

